/**
 * archivo que contiene la clase inventario
 */
class inventario{
    constructor(id,cantidad,existencia,libro){
       this.id = id;
       this.cantidad = cantidad;
       this.existencia = existencia;
       this.libro = libro;
    }
}  